name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: '3.11'
  DATABASE_URL: postgresql://codecoach:codecoach@127.0.0.1:5432/codecoach_test
  REDIS_ENABLED: 'false'
  ENVIRONMENT: testing
  JWT_SECRET_KEY: ci-test-secret-key-for-testing-only-32chars!!
  GROQ_API_KEY: ci-dummy-key

jobs:
  backend:
    name: Backend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: codecoach_test
          POSTGRES_USER: codecoach
          POSTGRES_PASSWORD: codecoach
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U codecoach -d codecoach_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r tests/test_requirements.txt

      - name: Lint with ruff
        run: |
          ruff check .
          ruff format . --check

      - name: Run unit tests
        run: |
          COVERAGE_FILE=.coverage.unit pytest tests/unit -v --cov=app --cov-report=xml:unit-coverage.xml

      - name: Run integration tests
        run: |
          COVERAGE_FILE=.coverage.integration pytest tests/integration -v --cov=app --cov-report=xml:integration-coverage.xml \
            --ignore=tests/integration/test_run_endpoints.py \
            --ignore=tests/integration/test_auth_supabase.py \
            --ignore=tests/integration/test_database_sync.py

      - name: Run contract + failure-simulation tests
        run: |
          pytest tests/contract tests/integration/test_external_failures.py -v --no-cov \
            --cov-report= --cov-fail-under=0

      - name: Run boundary condition tests
        run: |
          pytest tests/unit/test_boundary_conditions.py -v

      - name: Run security tests
        run: |
          pytest tests/security -v

      - name: Run smoke tests
        run: |
          pytest tests/integration -k "test_health_check_basic" -v

      - name: Combine coverage and enforce per-module budget
        run: |
          coverage combine .coverage.unit .coverage.integration
          coverage xml -o coverage.xml
          python ../qa/enforce_coverage_budget.py \
            --xml coverage.xml \
            --budget ../qa/coverage-budget.json \
            --baseline ../qa/coverage-baseline.json

      - name: Validate flaky-quarantine manifest
        run: |
          python tests/enforce_flaky_quarantine.py

  migration-test:
    name: Migration Tests
    runs-on: ubuntu-latest
    needs: backend
    defaults:
      run:
        working-directory: backend
    env:
      DATABASE_URL: postgresql+asyncpg://codecoach:codecoach@127.0.0.1:5432/codecoach_test

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: codecoach
          POSTGRES_PASSWORD: codecoach
          POSTGRES_DB: codecoach_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U codecoach"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r tests/test_requirements.txt

      - name: Run migration tests (isolated schema)
        run: |
          pytest tests/migrations -v --no-cov --cov-report= --cov-fail-under=0

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_OUTPUT
        id: pnpm-cache

      - name: Cache pnpm dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('frontend/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: TypeScript check
        run: pnpm run typecheck

      - name: Lint
        run: pnpm run lint

      - name: Run tests
        run: pnpm run test:run

  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [backend, frontend]

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Compose
        run: docker compose build --no-cache
